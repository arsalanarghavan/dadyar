# سیستم هوشمند شبیه‌سازی تصمیم‌گیری قضایی
# Judicial Decision-Making Simulator

<div dir="rtl">

## 📋 درباره پروژه

این سیستم برای **پایان‌نامه کارشناسی ارشد** با موضوع:

> **"طراحی چت‌بات هوشمند برای شبیه‌سازی تصمیم‌گیری قضایی در پرونده‌های مالکیت (غصب و خلع ید)"**

ساخته شده است. این سیستم یک **"Glass-Box AI"** است که فرآیند تصمیم‌گیری قضایی را به صورت شفاف و قابل توضیح شبیه‌سازی می‌کند.

</div>

---

## ✨ قابلیت‌های کلیدی

### 🔍 تحلیل هوشمند پرونده
- **استخراج خودکار اطلاعات**: شناسایی خواهان، خوانده، نوع پرونده، و واقعیات کلیدی
- **پردازش متن فارسی**: با استفاده از کتابخانه Hazm

### 📚 بازیابی مواد قانونی (RAG)
- **پایگاه دانش**: مواد ۳۰۸ تا ۳۲۷ قانون مدنی ایران (باب غصب)
- **جستجوی معنایی**: با استفاده از OpenAI Embeddings و FAISS
- **Hybrid Search**: ترکیب جستجوی معنایی و کلیدواژه‌ای

### 🧠 استدلال Chain-of-Thought
- **تحلیل گام‌به‌گام**: واقعیات → مواد قانونی → نتیجه‌گیری → حکم
- **شفافیت کامل**: نمایش تمام مراحل استدلال
- **سطح اطمینان**: محاسبه میزان اطمینان برای هر گام

### 🕸️ گراف تعاملی استدلال
- **نمایش بصری**: با استفاده از NetworkX و Plotly
- **چهار نوع گره**:
  - 🔵 واقعیات (آبی)
  - 🟢 مواد قانونی (سبز)
  - 🟡 نتیجه‌گیری (زرد)
  - 🔴 حکم نهایی (قرمز)
- **تعاملی**: Zoom, Pan, Hover tooltips

### ⚖️ صدور حکم نهایی
- **فرمت رسمی حقوقی**: خلاصه، واقعیات، تحلیل، حکم، قابلیت اعتراض
- **زبان تخصصی**: استفاده از واژگان حقوقی فارسی

### 🎨 رابط کاربری Premium
- **طراحی shadcn/ui**: زیبا، مینیمال، تیره
- **پشتیبانی کامل RTL**: برای متون فارسی
- **فونت Vazir**: رندر عالی متن فارسی

---

## 🚀 نصب و راه‌اندازی

### پیش‌نیازها

- **Python 3.9+**
- **OpenAI API Key** (برای GPT-4 و Embeddings)
- **حداقل ۴GB RAM** (برای FAISS)

### مراحل نصب

#### ۱. کلون کردن پروژه

```bash
git clone <repository-url>
cd PythonApp
```

#### ۲. ساخت محیط مجازی (اختیاری اما توصیه می‌شود)

```bash
python -m venv venv
source venv/bin/activate  # در لینوکس/مک
# یا
venv\Scripts\activate  # در ویندوز
```

#### ۳. نصب وابستگی‌ها

```bash
pip install -r requirements.txt
```

#### ۴. تنظیم API Key

فایل `.env` را بسازید (بر اساس `.env.example`):

```bash
cp .env.example .env
```

فایل `.env` را ویرایش کنید و `OPENAI_API_KEY` خود را وارد کنید:

```env
OPENAI_API_KEY=sk-your-api-key-here
OPENAI_MODEL=gpt-4-turbo-preview
OPENAI_TEMPERATURE=0.3
```

#### ۵. اجرای برنامه

```bash
streamlit run app.py
```

برنامه در مرورگر شما در آدرس `http://localhost:8501` باز می‌شود.

---

## 📁 ساختار پروژه

```
PythonApp/
├── app.py                          # فایل اصلی Streamlit
├── requirements.txt                # وابستگی‌های Python
├── .env.example                    # نمونه فایل تنظیمات
│
├── config/                         # تنظیمات و پیکربندی
│   ├── settings.py                 # Settings با Pydantic
│   └── prompts.py                  # Prompt‌های فارسی برای GPT
│
├── data/                           # داده‌ها
│   ├── legal_articles.json         # مواد ۳۰۸-۳۲۷ قانون مدنی
│   ├── sample_cases.json           # پرونده‌های نمونه
│   └── embeddings_cache.pkl        # کش embeddings (خودکار ساخته می‌شود)
│
├── modules/
│   ├── legal_engine/               # موتور تحلیل حقوقی
│   │   ├── openai_client.py        # کلاینت OpenAI
│   │   ├── knowledge_base.py       # پایگاه دانش + RAG
│   │   ├── entity_extractor.py     # استخراج اطلاعات
│   │   ├── reasoning_engine.py     # موتور استدلال
│   │   └── verdict_generator.py    # تولید حکم
│   │
│   ├── graph_builder/              # ساخت و رندر گراف
│   │   ├── reasoning_graph.py      # ساخت گراف با NetworkX
│   │   └── plotly_renderer.py      # رندر با Plotly
│   │
│   └── ui_components/              # اجزای رابط کاربری
│       ├── persian_utils.py        # ابزارهای RTL و فارسی
│       ├── input_form.py           # فرم ورودی
│       ├── analysis_view.py        # نمایش تحلیل
│       ├── graph_view.py           # نمایش گراف
│       └── sidebar.py              # نوار کناری
│
└── assets/
    └── styles/
        ├── main.css                # استایل اصلی (shadcn)
        └── rtl.css                 # استایل RTL
```

---

## 🎯 نحوه استفاده

### ۱. ورود اطلاعات پرونده

- به تب **"ورودی پرونده"** بروید
- می‌توانید از دکمه **"بارگذاری پرونده نمونه"** استفاده کنید
- یا اطلاعات پرونده را دستی وارد کنید:
  - شناسه پرونده
  - تاریخ (تقویم شمسی)
  - نام خواهان و خوانده
  - شرح کامل پرونده (حداقل ۵۰ کاراکتر)
- روی **"شروع تحلیل پرونده"** کلیک کنید

### ۲. مشاهده تحلیل

- به تب **"تحلیل قضایی"** بروید
- سیستم به صورت خودکار:
  1. اطلاعات را استخراج می‌کند
  2. مواد قانونی مرتبط را بازیابی می‌کند
  3. استدلال Chain-of-Thought انجام می‌دهد
  4. حکم نهایی را صادر می‌کند

### ۳. بررسی گراف استدلال

- به تب **"گراف استدلال"** بروید
- گراف تعاملی را مشاهده کنید:
  - موس را روی هر گره نگه دارید → جزئیات نمایش داده می‌شود
  - اسکرول → زوم
  - کلیک و کشیدن → حرکت در گراف

---

## 🔬 معماری فنی

### Stack Diagram

```
┌─────────────────────────────────────────┐
│        Streamlit UI (RTL/Persian)       │
│     shadcn-inspired Dark Theme          │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│         UI Components Layer             │
│  Input Form │ Analysis │ Graph │ Sidebar│
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│         Business Logic Layer            │
│  Entity Extractor │ Reasoning Engine    │
│  Verdict Generator │ Graph Builder      │
└─────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────┐
│          Data & AI Layer                │
│  OpenAI GPT-4 │ FAISS │ Legal Knowledge │
└─────────────────────────────────────────┘
```

### کامپوننت‌های کلیدی

1. **OpenAI Client**
   - مدیریت تماس با API
   - Rate limiting و retry logic
   - کش کردن پاسخ‌ها

2. **Knowledge Base (RAG)**
   - بارگذاری مواد قانونی
   - تولید embeddings
   - جستجوی معنایی با FAISS

3. **Reasoning Engine**
   - تحلیل گام‌به‌گام
   - محاسبه سطح اطمینان
   - ساخت زنجیره استدلال

4. **Graph Builder**
   - ساخت گراف با NetworkX
   - رندر تعاملی با Plotly
   - پشتیبانی از فونت فارسی

---

## 📊 نمونه خروجی

### ورودی نمونه:

```
خواهان ادعا می‌کند خوانده بدون اجازه وارد ملک مسکونی او شده
و از تاریخ ۱/۱/۱۴۰۰ تاکنون در آن سکونت دارد. خواهان تقاضای
خلع ید و پرداخت اجرت‌المثل را دارد.
```

### خروجی سیستم:

**اطلاعات استخراج شده:**
- خواهان: (از متن استخراج)
- خوانده: (از متن استخراج)
- نوع پرونده: غصب و خلع ید
- واقعیات کلیدی: تصرف بدون اجازه، سکونت از ۱۴۰۰، ...

**مواد قانونی بازیابی شده:**
- ماده ۳۰۸ (تعریف غصب) - ارتباط: ۹۵٪
- ماده ۳۱۵ (رد عین) - ارتباط: ۹۰٪
- ماده ۳۱۶ (خلع ید) - ارتباط: ۹۳٪
- ...

**حکم نهایی:**
محکومیت خوانده به تخلیه ملک و پرداخت اجرت‌المثل

---

## 🧪 تست برنامه

### تست دستی

1. بارگذاری پرونده نمونه:
   - کلیک روی "بارگذاری پرونده نمونه"
   - اجرای تحلیل کامل

2. تست با پرونده دستی:
   - وارد کردن شرح پرونده جدید
   - بررسی اعتبارسنجی فرم
   - مشاهده نتایج تحلیل

3. تست گراف:
   - بررسی نمایش صحیح گره‌ها
   - تست تعاملات (hover, zoom, pan)
   - بررسی رندر فونت فارسی

---

## 🐛 عیب‌یابی

### خطاهای رایج

#### 1. خطای API Key

```
❌ خطا در بارگذاری تنظیمات
```

**راه‌حل:**
- فایل `.env` را بررسی کنید
- `OPENAI_API_KEY` را تنظیم کنید

#### 2. خطای Import

```
ModuleNotFoundError: No module named 'xxx'
```

**راه‌حل:**
```bash
pip install -r requirements.txt
```

#### 3. خطای حافظه (FAISS)

```
MemoryError when loading FAISS index
```

**راه‌حل:**
- حداقل ۴GB RAM نیاز است
- Embeddings cache را پاک کنید: `rm data/embeddings_cache.pkl`

#### 4. مشکل فونت فارسی

**راه‌حل:**
- اطمینان حاصل کنید فایل‌های CSS بارگذاری شده‌اند
- Cache مرورگر را پاک کنید
- از مرورگر Chrome یا Firefox استفاده کنید

---

## 💡 نکات مهم برای استفاده در پایان‌نامه

### مزایای سیستم (برای دفاع)

1. **Explainable AI (XAI)**
   - شفافیت کامل در تصمیم‌گیری
   - قابلیت ردیابی استدلال
   - گراف بصری فرآیند

2. **Hybrid Approach**
   - ترکیب RAG و GPT-4
   - Semantic + Keyword search
   - Rule-based + AI-based

3. **Domain-Specific**
   - تخصصی برای حقوق ایران
   - پایگاه دانش قانون مدنی
   - پردازش متن فارسی

### محدودیت‌ها (برای صداقت علمی)

1. **وابستگی به GPT-4**
   - نیاز به API key
   - هزینه استفاده
   - کنترل محدود بر خروجی

2. **پایگاه دانش محدود**
   - فقط مواد ۳۰۸-۳۲۷
   - عدم پوشش رویه قضایی
   - نیاز به به‌روزرسانی دستی

3. **ارزیابی دشوار**
   - عدم Ground Truth قطعی
   - نیاز به ارزیابی متخصص
   - تفاوت‌های تفسیری

---

## 📚 منابع

- **OpenAI API**: https://platform.openai.com/
- **Streamlit**: https://streamlit.io/
- **FAISS**: https://github.com/facebookresearch/faiss
- **NetworkX**: https://networkx.org/
- **Plotly**: https://plotly.com/python/
- **Hazm**: https://github.com/roshan-research/hazm
- **shadcn/ui**: https://ui.shadcn.com/

---

## 📄 لایسنس

این پروژه برای مقاصد تحقیقاتی و آموزشی (پایان‌نامه کارشناسی ارشد) ساخته شده است.

---

## 👥 تماس و پشتیبانی

برای سوالات یا گزارش باگ:
- ایمیل: [your-email@example.com]
- GitHub Issues: [repository-url/issues]

---

## 🎓 استناد

اگر از این پروژه در تحقیقات خود استفاده می‌کنید، لطفاً به صورت زیر استناد دهید:

```
[نام نویسنده]. (۱۴۰۳). طراحی چت‌بات هوشمند برای شبیه‌سازی تصمیم‌گیری
قضایی در پرونده‌های مالکیت (غصب و خلع ید). پایان‌نامه کارشناسی ارشد،
[نام دانشگاه].
```

---

<div align="center">

**ساخته شده با ❤️ برای پیشرفت حقوق و فناوری**

نسخه ۱.۰.۰

</div>
